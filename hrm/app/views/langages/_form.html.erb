<%= form_with(model: langage) do |form| %>
  <% if langage.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(langage.errors.count, "error") %> prohibited this langage from being saved:</h2>

      <ul>
        <% langage.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :meta, style: "display: block" %>
    <%= form.text_area :meta %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
